# 🤖 DevOps Configuration

Generated by DevOps Agent for **SmitVgithub/3-tier-Application-via-Terraform**

## Summary

Created Terraform configuration for an AWS EC2 instance in a new `ec2-ubuntu/` directory with:

1. **main.tf** - Main configuration with:
   - Ubuntu 24.04 LTS AMI (dynamically fetched)
   - c6a.large instance type
   - Default VPC usage
   - 100GB GP3 encrypted root volume
   - Security group allowing ports 22, 80, 443 from anywhere
   - Mumbai region (ap-south-1)

2. **variables.tf** - Configurable variables for customization

3. **outputs.tf** - Useful outputs including public IP, instance ID, etc.

4. **README.md** - Documentation with usage instructions

## Files Created

- `ec2-ubuntu/main.tf`
- `ec2-ubuntu/variables.tf`
- `ec2-ubuntu/outputs.tf`
- `ec2-ubuntu/README.md`

## Required GitHub Secrets

| Secret | Description |
|--------|-------------|
| `AWS_ACCESS_KEY_ID` | AWS Access Key ID for authentication (if running via CI/CD) |
| `AWS_SECRET_ACCESS_KEY` | AWS Secret Access Key for authentication (if running via CI/CD) |

## Setup Instructions

1. Merge the PR to add the Terraform configuration to your repository
2. Navigate to the ec2-ubuntu directory: `cd ec2-ubuntu`
3. Configure AWS credentials: `aws configure` or set environment variables
4. Initialize Terraform: `terraform init`
5. Review the plan: `terraform plan`
6. Apply the configuration: `terraform apply`
7. **Important**: Add a key pair to the instance configuration if you need SSH access (add `key_name = "your-key-name"` to the aws_instance resource)
8. Consider restricting the security group CIDR blocks for production use

## How to Add Secrets

1. Go to your repository on GitHub
2. Click **Settings** → **Secrets and variables** → **Actions**
3. Click **New repository secret**
4. Add each secret listed above

## Need Help?

If you have questions about this configuration, feel free to ask the DevOps Agent!

---
*Generated automatically by DevOps Agent*
